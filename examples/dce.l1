fn main() {
    let n = 5000000;
    let t0 = time_ms();
    dead(n);
    let t1 = time_ms();
    empty(n);
    let t2 = time_ms();
    print(t1 - t0);
    print(t2 - t1);
}

fn dead(n) {
    let i = 0;
    while (i < n) {
        (i * i + 1) * (i + 2);
        i = i + 1;
    }
    return 0;
}

fn empty(n) {
    let i = 0;
    while (i < n) {
        i = i + 1;
    }
    return 0;
}
